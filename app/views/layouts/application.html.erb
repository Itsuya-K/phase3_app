<!DOCTYPE html>
<html>
  <head>
    <title>Friencher</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="background">
      <h1>Friencher</h1>
    </div>
      <nav>
        <ul>
          <li><%= link_to 'タイムライン', blogs_path %></li>
          <li><%= link_to '投稿', new_blog_path %></li>
          <% if logged_in? %>
            <li><%= link_to "Profile", user_path(current_user.id) %></li>
            <li><%= link_to "Logout", session_path(current_user.id), method: :delete %></li>
          <% else %>
            <li><%= link_to "Sign up", new_user_path %></li>
            <li><%= link_to "Login", new_session_path %></li>
          <% end %>
        </ul>
      </nav>
    </header>
    <div class="container">
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>

      <%= yield %>
    </div>

    <footer>
      <span><%= link_to 'フィードバック', new_contact_path %></span><span><%= link_to 'プライバシーポリシー', "#" %></span>
      <p>© 2018 Friencher</p>
    </footer>

  </body>
</html>
